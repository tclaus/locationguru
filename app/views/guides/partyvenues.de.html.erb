
  <div id="left">

    <h2>Partyräume mieten
    </h2>
    <small>Räume, Säle und Hallen - Für Parties aller Art. </small>
    <br>
    <p>
      Sie suchen ein Partyraum, eine Halle, eine gemütliche Location für Ihre Feier?
      Mit oder ohne Catering, mit vollausstattung oder selbstversorger - hier finden Sie was sie suchen.
    </p>
    <br>

    <div class="row" id="location_listing">
      <% if !@locations.empty? %>
        <%= render partial: 'locations/location_list' %>
      <% else %>
        <%= render partial: 'pages/no_locations_found' %>
      <% end %>
    </div>
  </div>

  <div id="right">
    <!-- google map -->
    <div id="map" data-mapdata='<%= raw @simple_locations.to_json.gsub("</", "<\\/") %>' class="maps-area-multiple">

      <%= javascript_include_tag('maps_location_list') %>
    </div>
  </div>
